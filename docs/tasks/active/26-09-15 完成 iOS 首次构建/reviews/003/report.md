# Independent review

Snapshot: 9caf4e4362f4f97a6acbf4a760e873b95fbcfd8cfaad0298fe90fe97cf3b3a2e
Conclusion: nonblocking

## Coverage
Required requirement/spec: 'requirements/docs/tasks/active/26-09-15 完成 iOS 首次构建/task.md' read in full (and its snapshot copy at 'snapshot/docs/tasks/active/26-09-15 完成 iOS 首次构建/task.md', sha256 7ab306de… identical); manifest.json spec_selections is empty, so no further Spec/requirements document was selected (docs/specs is an excluded path). Optional/context material read in the snapshot: BUILDING.md, deps/build_lame.sh, deps/build_ffmpeg.sh, deps/build_ish.sh, deps/prepare_alpine_rootfs.sh, src/ios/Info.plist, src/ios/Minis.xcodeproj/project.pbxproj (target sections, build configurations, baseConfigurationReference, INFOPLIST_KEY_* and PRODUCT_* settings), src/ios/Views/Settings/AboutView.swift, src/ios/Views/Settings/MemoryManagementView.swift (full file), src/ios/Agent/Sync/V2/ForceSyncHelper.swift; patches/base-to-worktree.patch (identical sha256 to patches/unstaged.patch; patches/staged.patch is the empty-file hash), request.md, status.json; host SDK evidence read from /Applications/Xcode.app/…/iPhoneOS27.0.sdk/…/SwiftUI.swiftmodule/arm64e-apple-ios.swiftinterface (ToolbarItemPlacement and *Builder.buildLimitedAvailability declarations).

## Limitations
No compilation, no test run, and no app execution were performed — this is a static, read-only review; nothing here is proof of runtime correctness (the task itself states real-device UI/sync testing is out of scope). The only primary evidence that the native dependencies/rootfs were really generated and that the Xcode 27 Debug generic/platform=iOS CODE_SIGNING_ALLOWED=NO build actually reached exit 0 / BUILD SUCCEEDED sits in the excluded paths docs/tasks/active/26-09-15 完成 iOS 首次构建/evidence/*.log.gz and verification.md/task.checkpoint.json, so acceptance items 1 and 2 are accepted only as author assertions; the snapshot does corroborate the iSH submodule worktree at the pinned commit de124dd6 with build-ios artifacts (libish.a, libish_emu.a, libfakefs.a, vdso/arm64/libvdso.so.elf), and no build-native/ entry is visible in that recorded status, so I could not confirm or refute prepare_alpine_rootfs.sh having produced alpine-rootfs.zip. MinisTests/MinisUITests were not read (excluded) and were not executed, so no test result for the toolbar change exists in this review. All excluded paths (including src/ios/Configs with the untracked ProviderCustomization.xcconfig that is referenced as baseConfigurationReference by the project-level Debug/Release configurations at project.pbxproj:3028 and 3094, ShareExtension/FileProvider/AgentWidget Info.plist files, Localizable.xcstrings, entitlements, deps/ish, deps/lame-3.100, deps/ffmpeg-patch, deps/proot, docs/specs) were deliberately not inspected, so extension display-name precedence and any runtime consumer of CFBundleDisplayName/CFBundleName could not be resolved. Toolbar rendering with an empty ToolbarItem (iOS 16 or iCloudSyncEnabled == false) was not observed at runtime; static reading suggests an empty item renders nothing but a residual cosmetic risk (stray toolbar slot) cannot be excluded from source alone. The pre-fix compile error itself was not reproduced with xcodebuild.

## R1
- severity: low
- requirement: task.md '编译修复与审查范围': minimal fix must keep behaviour ('iOS 17+ 且启用同步时保持原菜单和异步动作，iOS 16 或同步关闭时不显示菜单', '不得提高最低部署版本') — check that the compile fix preserves behaviour without regressions.
- location: src/ios/Views/Settings/MemoryManagementView.swift:62-76 (ToolbarItem at line 63); comparison site src/ios/Views/Settings/MemoryManagementView.swift:198-201 (.topBarTrailing in MemoryFileEditView).
- trigger: Building the Minis app with the iPhoneOS 27.0 SDK for deployment target iOS 16.0, and rendering the Memory toolbar at runtime on any OS version.
- evidence: Snapshot line 63 now reads 'ToolbarItem(placement: .navigationBarTrailing) {' with the gated 'if #available(iOS 17.0, *), iCloudSyncEnabled {' moved inside the item's ViewBuilder (lines 64-75); the patch hunk @@ -60,8 +60,8 @@ in patches/base-to-worktree.patch shows exactly that swap (removing the outer 'if #available(iOS 17.0, *)' + '.topBarTrailing' pair). SDK interface: ToolbarItemPlacement.topBarTrailing is '@available(iOS 14.0, tvOS 14.0, watchOS 10.0, *) @backDeployed(before: iOS 17.0)' and its body is literally '.navigationBarTrailing', while ToolbarItemPlacement.navigationBarTrailing is '@available(iOS, introduced: 14.0, deprecated: 100000.0, message: "use topBarTrailing instead")' (iPhoneOS27.0.sdk SwiftUI.swiftinterface lines ~7579-7586 and ~7608-7617). Behaviour is therefore equivalent (same placement value, same visibility condition, forceSyncMemory() still @available(iOS 17.0, *) and only reachable inside the guard; IPHONEOS_DEPLOYMENT_TARGET stays 16.0 at project.pbxproj:3194/3248 and sync is not removed).
- verification: Read the whole final snapshot file (not just the diff), the complete patch artifact, and the iPhoneOS27.0.sdk SwiftUI.swiftinterface declarations to confirm the placement identity, the iOS 14+/back-deployed availability, and that the deprecation is the 100000.0 (no-warning) flavour; cross-checked that the only two overloads of ViewBuilder.buildLimitedAvailability that could be selected for this content are the generic View one (iOS 13) and the '@_disfavoredOverload … any SwiftUI.ToolbarContent' one gated at iOS 17.5 (interface lines ~21636-21645), so the iOS 17.0 guard inside the item is not re-triggering the 17.5 ToolbarContentBuilder requirement that caused the original failure. No build/run was executed to confirm the compiled result.
- certainty: high for the placement equivalence and for 'no build/runtime regression expected'; medium for the absence of any residual cosmetic toolbar-slot change on iOS 16 / sync-off, which only a view-rendering check could settle

## R2
- severity: low
- requirement: task.md '编译修复与审查范围': review whether the pre-existing MinisX rename '意外影响构建标识' (accidentally affects build identifiers) and whether the rename itself stays consistent.
- location: src/ios/Info.plist:5-6 (CFBundleDisplayName = MinisX); src/ios/Minis.xcodeproj/project.pbxproj:3271, 3298 ('Share to MinisX'), 3325, 3352 ('MinisX Files'), 3379, 3406 ('MinisX Activity'); src/ios/Views/Settings/AboutView.swift:26 (Text(verbatim: "MinisX")) and :33 (tagline still 'Minis is Your Fully Local…'); Info.plist:51-329 still 'Minis uses …' usage strings and CFBundleURLName 'Minis Share'.
- trigger: Packaging the Minis app plus MinisShare/MinisfileProvider/AgentWidgetExtension, and any user-visible surface that shows the app name.
- evidence: Diff touches only display-name strings and one SwiftUI Text: bundle identity settings are untouched — PRODUCT_BUNDLE_IDENTIFIER remains com.openminis.app / .ShareExtension / .FileProvider / .AgentWidget / .MinisTests / .MinisUITests, PRODUCT_NAME = $(TARGET_NAME), and PBXNativeTarget names (Minis, MinisShare, MinisFileProvider, AgentWidgetExtension, MinisTests, MinisUITests), productReference names and schemes are unchanged (project.pbxproj:2124-2250, 2934-3416); the app target has no INFOPLIST_KEY_CFBundleDisplayName, so the plist value is the only source. However the three extensions set their display name only through INFOPLIST_KEY_* while their own Info.plist files (ShareExtension/, FileProvider/, AgentWidget/) are excluded paths, so whether the built extension bundle really shows 'MinisX …' cannot be confirmed, and 'Minis' branding still remains in the app's own usage-description strings, the CFBundleURLName and the About tagline.
- verification: Compared the patched files against the exclusion list in request.md/manifest.json; grepped project.pbxproj for PRODUCT_BUNDLE_IDENTIFIER / PRODUCT_NAME / INFOPLIST_KEY / PBXNativeTarget to confirm no identifier, product name or target/scheme name changed. The INFOPLIST_KEY_* vs Info.plist-file precedence question and the excluded extension plists could not be resolved from the reviewed material.
- certainty: high that no build identifier (bundle id, product/target name, entitlement key, URL scheme declared in the reviewed plist) changed; medium that the extension-level display-name rename is effective; high that the rename is cosmetically incomplete in user-visible strings
