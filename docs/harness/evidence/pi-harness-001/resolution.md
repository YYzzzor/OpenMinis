# 首轮实际 Harness 审查处理记录

日期：2026-09-11。审查模型由 Pi 自报为 deepseek / deepseek-flash，底层版本未独立核实。
原始报告：[pi-review.md](pi-review.md)。快照：`2dd42e97ef656aac4512b89e1713ce90b2f99968624f9140e238ab72d3444649`。
接收报告后、修改工作区前，主 Agent 执行 `review.py check <原始 bundle> --repo <当前仓库>` 成功；报告绑定匹配。
完整原始材料保存于 [bundle.tar.gz](bundle.tar.gz)，解压后的 review 目录可独立执行 check。归档 SHA256：`9ccbc6e07226b8d665daf3e7bdb8cf0872cdd002033bd46e9614dd76ee0328fa`。
这是用户现有 Pi 会话产生的手工报告，没有伪造适配器的 status.json 或成功状态。

## 逐项处置

- R1：接受交接遗漏。首次请求未显式列出必读提案 003，虽 snapshot 有绑定且 Pi 实际读到，仍不满足明确传递清单的要求。下一轮补入提案 002/003，并明确选择 spec-context 的必读小节。不新增自由文本任务清单解析器；现有批准设计由主 Agent 判断依赖，自动语义校验不属于已实现保证。
- R2：接受并更新任务、验证记录，区分基础版本 20 项、Spec 版本 27 项与当前 29 项。移除当前“首次审查待授权”的过时表述。机械状态匹配不证明记录语义正确，既有恢复流程已要求读正文。尚未完成的真实任务试用不因收到报告而勾选。
- R3：保留为已知限制，不认定实际越界缺陷。现有手工指令已要求仅使用快照，报告也声明遵守。绝对来源路径用于追溯；工具白名单不等于文件系统沙箱，不能证明所有读取路径。下一轮继续明确仅读 bundle，暂不改变共享规则或增加沙箱。
- R4：已修复。JSON 仅将不可编码 surrogate 转成反斜杠 Unicode 转义，保持普通 Unicode 字节不变，避免 ensure_ascii 全量变更令旧快照摘要失效。请求文件同样允许显示转义路径。新增非 UTF-8 文件名的快照、需求复制、校验和变化检测测试。
- R5：已补回归。模拟成功报告后，分别修改及清空 report.txt/json/md，确认 check 与 export 都拒绝，且不产生导出目录。

## 验证与未关闭范围

主 Agent 运行 29 项测试全部通过。首轮实际快照与历史 pi-smoke-001 均仍通过独立完整性校验。修复改变了实现与任务记录，原始报告只覆盖旧版本；针对性复审材料将准备于 `/tmp/openminis-pi-handoff-gjhxqwrc/followup-002`，尚未得到第二轮结论。

报告主动运行了临时测试，超出交接时静态阅读要求；主 Agent 独立重跑测试，不把报告中的执行声明当作独立核实的工具日志。此偏差在当前交接中纠正，不据此自动修改影响后续任务的规则。

其他观察（完整调用 prompt 留存、行号端点说明、额外边界测试）作为后续候选，未因本轮审查自动扩大实施范围。Pi 恢复/技能发现、真实 OpenMinis 任务试用仍待验证。
