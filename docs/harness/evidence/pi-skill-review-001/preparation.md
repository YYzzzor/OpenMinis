# Pi review-task 实际执行验证准备

状态：prepared，等待用户现有 Pi 会话执行。
输入为独立临时 Git 仓库的合成 HTTP 响应分类任务，不含 OpenMinis 应用源码。已完成固定快照准备及 freshness 校验；附带当前 review-task 原文副本。已有 2 项样例测试通过，输出保存于 local-tests.txt；测试通过不代表业务约定完整满足。

本轮验证显式调用 skill 的独立审查角色：读取 skill、遵守固定材料与只读范围、按要求输出有依据且绑定快照的发现、说明未验证范围、不递归启动审查、不修改实现、不直接决定验收。自动发现已有正常恢复会话自报证据，本次显式提供 skill 副本入口，不把它当作自动发现机制的新证明。

本环境执行 pi --help 在模型调用前因 ~/.pi/agent/trust.json.lock 写入被只读文件系统拒绝，没有产生外发调用，也不是自动审批审核拒绝。改用用户现有 Pi 会话执行。

待结果返回后，主 Agent 核对快照/输入未改、发现是否符合任务及代码、角色与范围是否遵守，再记录结论。样例评估标准不以固定发现数量代替正确性。
