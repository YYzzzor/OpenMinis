# Pi 正常新会话恢复：主 Agent 评估

日期：2026-09-11。输入证据为用户在本会话粘贴的 Pi 完整回复；本文件为摘要与评估，不冒充原始 Pi 会话日志。恢复检查前记录的检查点保存在 before.checkpoint.json，HEAD 为 8399e1f9889f0d04629d560b5b0f881123524b78，分支 ios-annotated。

## 观察与核实

用户按演练要求在新 Pi 会话仅提供任务入口，报告正确识别目标、实现审查闭环、尚未勾选验收、规则审批与提交边界。Pi 报告 inspect 为 match。主 Agent 在写入本轮验证记录前独立执行 inspect，同样为 match，说明检查点覆盖的文件与 Git 状态没有可检测漂移；不能证明绝无临时写入后还原或忽略文件变化。

Pi 自报项目 AGENTS.md 与两项 skill 的路径/描述在启动上下文中自动出现，随后读取 resume-task 正文并执行恢复核查。该结果支持本次 Pi 环境的 skill 发现与恢复执行；没有原始启动/工具日志可独立证明加载机制，不外推为所有 Pi 配置。review-task 仅被发现，正文未读取，未验证其原生执行流程。

## 判断与范围

正常、状态一致场景的 Pi 新会话只读恢复初步通过。7 项未勾选验收仍按整体任务保留，不因本次演练全部勾选。过时检查点、待审批提案的实际行为和恢复后开发续接未在此轮验证。

Pi 末尾再次列“验证 Pi 恢复”为下一步，是沿用演练开始前的任务记录；本轮明确只读、不更新任务，停止执行符合要求。主 Agent 现在更新进展，下一步转为 Codex 真正的新对话恢复，不重复本轮演练。

用户额外要求在 Pi 验证后测试 Codex 跨对话恢复。此前 sub-agent 验证不替代该测试。正常场景通过后，再在隔离环境验证状态漂移与审批边界。
