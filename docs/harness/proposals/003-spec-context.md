# 提案 003：Spec 按需小节上下文

状态：implemented；本地验证通过，实际 Pi 小节审查待验证。
日期：2026-09-11

## 审批与原因

助手已提出索引常驻、正文按需、默认按完整小节读取，区分必读与参考，保留完整原文快照及版本绑定。用户明确要求「关于『索引常驻、正文按需读取、默认按小节读取』的设计，也加入当前的 Harness 框架」。该请求授权实施此设计，不扩展既有外发授权。

## 修改范围

- AGENTS.md 增加简短索引及按需读取入口。
- 设计、任务格式、操作说明增加必读/参考清单及完整原文绑定。
- review.py 扩展必读小节与参考小节输入，完整源文件仍进入 requirements 快照；请求注入相关内容而非默认所有正文。
- 增加小节提取和集成测试：代码围栏、嵌套、歧义、缺失标题、原文变化与旧接口兼容。

不修改三份上游 Spec 正文，不根据旧实现说明制造新规范。两项技能通过项目 AGENTS.md、任务清单及操作说明使用这项机制，无需另建技能。

## 影响与验证

适用于实现、委派、恢复和审查的规范读取。减少无关正文，同时要求发起者保留依赖条件与例外。风险是选择遗漏；以完整原文可回读、必读清单和实际覆盖报告控制，不能宣称工具自动保证语义完整性。

小节选择改变时重新准备审查材料。格式与模拟测试通过不代表实际 Pi 必读行为已验证；实际项目审查仍遵循既有外发审批。
