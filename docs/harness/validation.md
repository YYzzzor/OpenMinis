# 第一版验证记录

日期：2026-09-11。提案 001 已批准并实施；基础机制实现，整体任务仍 active。

## 本地验证

- `python3 -B -m unittest discover -s scripts/harness -p 'test_*.py' -v`：当前 30 项通过（第二轮 Pi 意见处理后）；基础机制阶段为 20 项，Spec 实施后为 27 项。
- OpenMinis 实际仓库快照准备成功（排除 deps/ish、deps/proot），未发生源码修改。
- 使用 `/usr/bin/python3` 执行自带 skill-creator 的 `quick_validate.py`：resume-task、review-task 均通过。默认 Conda Python 缺少 PyYAML，改用已有系统 Python，无新增依赖。
- 恢复测试覆盖检查点一致、Git 状态相同但内容变化、暂存与新增文件、未完成 Git 操作、替换写入失败仍保留旧记录、只读检查不修改待审批任务。
- 审查测试覆盖 staged/unstaged/untracked 快照、删除文件、需求变化、暂存变化、篡改、显式子模块排除、失败/超时/残缺/无法审查、报告绑定与导出。

这些测试使用临时 Git 仓库与模拟 Pi，证明脚本行为，不证明模型判断正确。

## 独立恢复前向验证

独立 sub-agent 在临时仓库按真实场景执行恢复 skill：检查点后存在源码修改，任务中有未批准规则提案。inspect 返回 changed/退出 2，源码、任务和旧检查点未被改变。执行者识别记录过时，选择先验证已有实现再更新进展，未把提案作为规则。

普通条件表达式解释场景无需恢复流程：这是独立执行者的语义判断，未开展自动选择率评测。此项验证不是 Pi 执行结果。

## 真实 Pi + DeepSeek 小样例

- Pi：0.85.1；provider：deepseek；model：deepseek-v4-pro。
- 固定需求要求 cancel 为真返回 cancelled，否则 done。未提交代码故意交换两个分支。
- 真实只读调用成功，返回 blocking/R1，正确指出两个分支均违反需求，并注明未执行运行测试。
- 原始材料、请求、快照摘要、原始报告、渲染报告与调用状态保存在 [pi-smoke-001](evidence/pi-smoke-001/report.md) 同目录。来源目录为临时仓库，副本保留完整快照可用于核实报告；manifest 的原始路径作为来源证据，不声称临时仓库永久存在。
- 未向模型提供预期问题答案。该小样例证明调用链和一个意图偏差案例有效，不证明对实际复杂实现的审查收益。

## 实际 Harness 实现审查

主 Agent 已检查脚本和边界，测试通过。已准备仅包含 Harness 脚本、技能、设计文档的临时快照，未包含应用源码或凭据。

向 DeepSeek 发送该实际项目快照的调用被自动审批系统拒绝，理由是缺少针对这批项目文件外发到 DeepSeek 的明确授权。当时未执行该调用，也未绕过拒绝。随后用户在自己的 Pi 会话使用 deepseek-flash 完成首轮实际审查，报告已返回。主 Agent 接收时验证快照与工作区一致，逐项处理 5 条非阻断意见；原始报告、完整快照归档及处置见 [处理记录](evidence/pi-harness-001/resolution.md)。本轮未另行调用外部模型。

## 尚未完成的验证

- 真实任务中的审批/纠偏效果与成本观察；隔离审批流程演练与实现审查已完成。
- 真实应用恢复续接与跨工具异常场景。双方正常恢复、独立隔离状态漂移、Pi 显式审查与两阶段审批演练已初步通过，详见下文。
- 两三个真实 OpenMinis 任务的使用效果与成本观察：首个真实分析任务已通过；用户决定其余真实代码修改与 iOS 运行验证迁移至 Mac 后再开展。
- 操作系统断电耐久性、进程树强制终止、排除子模块内部行为不在已通过测试范围内。

共用规则提案的审批仍是人工授权记录，由 agent 执行，不是密码学签名或自动审批服务。脚本测试不代表这些语义边界已被程序强制证明。

## 提案 003：Spec 小节上下文

已加入 AGENTS.md 常驻索引、任务必读/参考清单、本地小节阅读工具，以及审查准备参数。三份上游 docs/specs 正文保持不变。

本地验证覆盖祖先引言和例外保留、围栏伪标题、嵌套与歧义、缺失标题失败、必读与参考内容分离、原文完整复制和过时检测、原有全文接口兼容。实际读取 minis-url-scheme 的 Session Model，成功保留文档 Draft 状态和完整子小节。

主 Agent 检查发现参考小节正文可能经 manifest 提前进入上下文，已将清单限制为标题、范围和摘要；正文仅保留在原文快照中，必读正文另置 request.md。回归测试确认两个入口都不含参考正文。该阶段 27 项测试全部通过；首轮审查修复后新增 2 项回归，该阶段共 29 项通过。

首轮 Pi 报告列出了实际阅读范围，但首次交接使用全文参数，遗漏显式列出提案 003。已准备在下一轮补齐必读依据并使用小节参数；不能以首次全文阅读证明按小节交接的实际效果。

## 第二轮复审与增量修复

Pi 静态复审确认原 R1/R2/R4/R5 已解决。主 Agent 核对绑定并处理新意见：不新增复审 CLI 参数，使用已有 --spec 绑定处理记录；复现并修复 surrogate 报告渲染失败；测试以文件创建能力判断是否跳过，不将 POSIX 等同于支持任意字节文件名。当前 30 项测试全部通过，Linux 无跳过；未验证 macOS。该阶段新代码尚未获独立复核，现已由下述最终复核关闭。详见 [第二轮处理记录](evidence/pi-harness-002/resolution.md)。

用户指明模型为 DeepSeek-V4.1-flash；报告中的 deepseek-flash 是 Pi 自报别名。报告与处理记录分开保留，完整材料一并归档。此轮不代表 Pi 技能发现、跨会话恢复或真实应用任务试用已完成。

## 提案 004：独立核实审查意见

用户明确批准后，同步项目入口、review-task skill、设计、操作说明和记录格式。主 Agent 检查 blocking 不自动否决、no_findings 不自动通过、不采纳需证据、实质疑点不关闭四项语义一致；文档差异检查与 skill 格式校验通过。未修改运行代码，不重复运行 30 项脚本测试，也不把格式检查当作跨模型行为验证。历史复审报告不改写；followup-003 早于此规则更新，仍只绑定其原快照。

## 最终针对性复核

固定快照 e8a6cfb1… 经主 Agent 接收时 check --repo 校验成功。Pi 静态复核 N2/N3，并确认 N1 的必读交接；主 Agent 结合先前运行验证关闭三项，未采纳扩大两项额外断言的建议，理由见 [最终处理记录](evidence/pi-harness-003/resolution.md)。本轮仅更新记录，未重复运行 30 项测试。原始报告与完整材料已分别归档。

实现审查闭环完成，不等于整体 Harness 验收完成。Pi 新会话恢复、技能发现、真实任务试用以及未验证平台限制仍保持开放。

## Pi 正常新会话恢复

用户返回新 Pi 会话的只读恢复结果：目标、进展、未完成验收和审批边界识别正确，inspect 返回 match。主 Agent 更新记录前独立 inspect 也为 match。Pi 自报启动时发现 AGENTS.md 与两项 skill，实际读取 resume-task；review-task 仅发现，未执行。判定正常只读恢复初步通过，证据与限制见 [评估记录](evidence/pi-recovery-001/assessment.md)。

用户要求接着验证 Codex 真正的新对话恢复，已列入计划。此前 sub-agent 演练不替代这项检查；恢复后实际续接、状态漂移与审批/纠偏案例仍待验证。

## Codex 正常新对话恢复

用户返回真正新 Codex 对话的只读恢复结果，目标、进展、历史证据与未完成验收识别正确；resume-task 被读取并用于恢复，review-task 未执行。主 Agent 更新记录前独立 inspect 为 match，当前状态与此前检查点一致。正常新对话只读恢复初步通过，详见 [评估记录](evidence/codex-recovery-001/assessment.md)。

双方正常场景已有新会话证据；未验证恢复后开发续接、过时状态和审批/纠偏实际行为，不据此勾选整体恢复验收。本轮仅记录结果，未运行测试或修改共享规则。

## 独立隔离状态漂移与续接

用户授权开展下一项验证后，在临时仓库构造旧检查点、已有正确未提交修复、未跟踪笔记与未批准提案。独立 sub-agent 完成恢复及本地验证续接，纠正任务而未覆盖实现或启用提案。主 Agent 独立核对仅任务与检查点变化，HEAD/分支/暂存区不变，最终 inspect 为 match。本案例通过，见 [评估记录](evidence/recovery-drift-001/assessment.md)。

本轮未重跑 OpenMinis 的 30 项脚本测试：未修改脚本。隔离 fixture 由执行者运行 2 项测试；主 Agent 独立检查四种输入与前后文件。真实客户端异常恢复、主动规则调整审批与实际应用开发仍未被该案例证明。

## Pi review-task 显式执行

Pi 对固定合成样例读取指定 skill 后独立审查，发现真实业务偏差。主 Agent 核对绑定，并穷举 400 个有效输入确认全部 99 个错误与 R1 一致。测试遗漏 R2 部分接受，范围外输入 R3 不采纳。原始报告、完整材料和 [处理记录](evidence/pi-skill-review-001/resolution.md)已归档。

显式独立审查角色初步通过，不代表自动触发或全部 skill 角色已验证；本轮模型版本未确认，工具边界依据报告自述与快照校验。样例缺陷有意保留，不开展修复或重复审查。

## 主动纠偏与共享规则审批演练

独立执行者在合成仓库中纠正静态审查冒充集成验证的记录，主动提出具体共享指导方案并等待批准。评估者核对后提供只限临时仓库的模拟批准；执行者精确实施批准文本，未把规则更新当成产品验收通过。主 Agent 核对前中后文件、HEAD/分支/暂存区及最终检查点，案例通过。详见 [评估记录](evidence/rule-approval-001/assessment.md)。

模拟批准不是 OpenMinis 真实授权；本轮仅在实际项目归档证据与更新任务，不修改实际共享规则。尚无真实应用任务试用的效果/成本证据，不因隔离案例通过宣称 Harness 整体验收完成。

## 用户决定：迁移 Mac 后继续运行验证

用户确认本机是 WSL，暂缓剩余真实代码修改/实现验证，待整个项目迁移到 Mac 后继续。首个真实分析试用通过的结论保留，整体 Harness 验收不标完成。恢复时先核对迁移后的代码、未提交记录、工具与构建环境；历史临时路径不可作为新环境必然存在的依赖。本次只记录延期，不修改共享规则、不执行迁移或提交。
