# 26-09-15 设置 MinisX 默认系统版本
状态：done

## 目标与验收
用户明确指定项目名称 MinisX、最低 iOS26。同步项目默认说明、所有应用/扩展/测试部署版本及原生依赖默认值。
保持现有标识、图标与应用逻辑。默认验证设备仍为已安装的 iPhone18 Pro/iOS27.0，最低部署版本26.0。

## 恢复信息
分支：feat/minisx-branding；基准HEAD a4c5dd5；任务前工作区干净。
已完成：重建模拟器依赖和应用并核验部署版本，用户手动运行验收通过。用户已授权将本任务修改提交到当前分支；未要求推送。

## 验证
用户明确授权的项目默认规则已写入 AGENTS.md，供后续对话读取。

- `bash scripts/build_ios_simulator.sh` 完整重建依赖与应用成功，日志 `/tmp/MinisX-ios26-build.log`。
- 应用及三个扩展的产物 Info.plist 均为 MinimumOSVersion 26.0；名称分别为 MinisX、MinisX Activity、MinisX Files、Share to MinisX。
- vtool 确认应用及七个 FFmpeg 框架的 Mach-O 平台为 IOSSIMULATOR、minos 26.0、SDK 27.0。
- 项目十四处部署版本配置均为 26.0；plutil 校验及 git diff --check 通过。
- 构建阶段未执行 iOS 26 运行测试或真机构建；后续用户手动运行结果见下方。
- 本次为用户明确要求的默认配置调整，由主代理检查并验收，未启动 Pi 审查。

## 追加运行测试
- 用户要求运行测试。只读确认 iOS 26.4 的 iPhone 17 Pro（A6D580D2-1D43-4307-BD75-8D7E3504E282）已安装、处于 Shutdown；26.0 runtime 不可用。
- 启动模拟器被自动审批连续拒绝，原因是审批模型容量不足（Selected model is at capacity），未实际执行启动、安装或运行。
- 用户随后指定已启动的 iPhone 17 Pro Max/iOS 26.4（9EAE1ADB-6480-44F4-BD9E-B4BC6C23DB3B），要求仅安装，由用户自行运行。
- 已成功执行 simctl install，退出码 0；get_app_container 确认 com.openminis.app 已安装。未启动应用。
- 用户已反馈自行运行测试完成、没有问题。运行验收依据为用户在 iPhone 17 Pro Max/iOS 26.4 上的手动测试；主代理未独立执行功能回归，也未验证精确 iOS 26.0 runtime。
- 当前任务完成；配置、文档及本记录纳入同一提交，提交前 HEAD 为 a4c5dd5。后续新功能先核验当前分支与工作区。
