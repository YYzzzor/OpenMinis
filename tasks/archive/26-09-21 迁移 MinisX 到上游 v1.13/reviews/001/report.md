# Independent review

Snapshot: 1ba360fa2bbd1f9a9d0888199e0568e03639bce21cb620de31c91c3da287a059
Conclusion: nonblocking

## Coverage
Required (read in full): requirements/tasks/active/26-09-21 迁移 MinisX 到上游 v1.13/task.md; requirements/AGENTS.md; requirements/docs/specs/ios-calendar-recurrence.md; requirements/tasks/active/26-09-21 迁移 MinisX 到上游 v1.13/verification.md; .../calendar-results.json; .../harness-validation.md; .../upstream-bug-comparison.md. Required excerpts of requirements/docs/specs/ios-device-data-capabilities.md read as delivered: heading 1 '文档定位与核查基线', heading 2 'Agent 接口与苹果系统 API 的关系', heading 3 '个人数据读写', heading 6 '授权、缺失能力与容易混淆的范围'. Also inspected as evidence: patches/base-to-worktree.patch (CalendarOffload.m/.h, RemindersOffload.m, project.pbxproj, Info.plist, Localizable.xcstrings, ContentView.swift/SoulStore/InfoPlist.strings branding hunks) and snapshot source for snapshot/src/ios/NativeOffloads/CalendarOffload.m, CalendarTests/CalendarRecurrenceTests.m, RemindersOffload.m, SoulStore.swift. Optional references (requirements/docs/specs/ios-sandbox-ish-summary.md sections 1,2,4) were NOT read (optional per request).

## Limitations
No build, simulator launch, XCTest or Pi harness run was executed by this reviewer; all runtime results (32-item MinisCalendarTests suite, Harness 41-pass/1-skip, Xcode build, simulator logs) are accepted only from the task's records. The raw artifacts those records cite (calendar-26.4-final.log, calendar-27.0-003.log, build-for-testing-002.log, built-app-identity.json, branding-validation.json, harness-tests-final.log, harness-cli-smoke.json, the xcresult bundles) are not present in this review snapshot, so the claimed 28/4 counts and build/identity claims could not be independently reproduced or checked. Task records under tasks/ and docs/tasks/ other than the selected files are excluded from the snapshot, so Harness migration completeness (claimed 247 files, tasks/archive preservation, 16-step CLI drill, 11 historical bundle references) and link integrity could not be verified, and the untracked Harness additions (scripts/harness/*.py, test_*.py, .agents/skills/*, docs/harness/*) were not read line-by-line. Required Spec dependencies named in task.md's Spec 阅读清单 were not delivered with the snapshot and were not read: docs/harness/record-formats.md (mandatory) and .agents/skills/review-task/SKILL.md (mandatory for review), and docs/specs/debug-server-api.md (on-demand). deps/ish and deps/proot are excluded submodules and were not reviewed. User-accepted residual failures remain: the 4 weeksOfTheYear tests (testWeeksOfYear, testPositiveWeeksOfYear, testSetPositionsAcrossSelectors yearly-week combination, testNativeWeekNumberReference) fail on iOS 26.4 and 27.0 and direct EventKit control also fails; recurrence_warnings flags this but real-device behavior is unverified, so recurrence for annual week numbers is not proven and no runtime correctness for any path is established by this review. No real-device, signing, iCloud-upgrade, real-model/API-key chat, or full old-bug UI reproduction was available; the two old bugs (thinking commit-boundary and math fallback) are documented as only partially present in v1.13 and intentionally not ported.

## R1
- severity: low
- requirement: task.md: '保留 v1.13 原有日历和提醒事项能力' and the migration's validation principle of rejecting rather than silently dropping unsupported recurrence input; ios-calendar-recurrence.md: '本次 --recurrence 高级参数只用于日历 create，没有扩展到提醒事项'.
- location: snapshot/src/ios/NativeOffloads/RemindersOffload.m reminders_handler (create->calendar_cmd_remind, update->calendar_cmd_update_reminder); snapshot/src/ios/NativeOffloads/CalendarOffload.m calendar_cmd_remind / calendar_cmd_update_reminder around lines 1690-1795, versus the new guard in calendar_offload_handle around lines 2023-2033 (patch hunk '@@ -1692,6 +2023,17 @@').
- trigger: Run `apple-reminders create --recurrence weekly ...` or `apple-reminders update --id <id> --recurrence weekly`.
- evidence: calendar_cmd_remind/calendar_cmd_update_reminder obtain the rule only via build_recurrence_rule, which reads `--recur` with noff_find_arg and returns nil when `--recur` is absent; there is no rejection of `--recurrence*`. I verified the delegation in RemindersOffload.m has no `--recurrence` check, whereas CalendarOffload.m's calendar_offload_handle rejects any argv token prefixed with '--recurrence' for every non-create `apple-calendar` subcommand (list/update/remind/update-reminder).
- verification: Read-only source inspection of both files and the patch hunk; not executed. Because the advanced family is ignored, the call returns ok:true with a non-recurring reminder, i.e. exactly the 'saved a one-off the user believes repeats' outcome the code rejects elsewhere.
- certainty: high

## R2
- severity: low
- requirement: task.md: '既有用户自定义助手名称和形象仍按用户配置显示；默认 Minis 名称按现有规则显示 MinisX'.
- location: snapshot/src/ios/Agent/Session/SoulStore.swift:513 (SoulMetadata.displayName) and its consumers snapshot/src/ios/Views/ContentView.swift (soulName initialization and .soulMdChanged handler), snapshot/src/ios/Agent/MessageList/CollectionViewMessageListV3.swift, snapshot/src/ios/Views/Settings/SoulSettingsView.swift (Text/TextField bindings), snapshot/src/ios/Agent/Background/AgentLiveActivityManager.swift.
- trigger: A user who has deliberately set their assistant name to exactly the string 'Minis' (optionally via SOUL.md frontmatter) opens the app, chat header, settings preview, or the Soul Settings name field.
- evidence: displayName returns 'MinisX' whenever `name.isEmpty || name == 'Minis'`, and every display site now routes through displayName (or an equivalent `name == 'Minis' ? 'MinisX' : name` binding in SoulSettingsView). The default and a user-authored 'Minis' are indistinguishable in the stored metadata, so the mapping also rewrites an intentional user customization rather than only the shipped default.
- verification: Read-only source inspection; not executed. On-disk identity (SOUL.md name field) is not rewritten unless the user edits the field, but the rendered name is changed for that user.
- certainty: medium
