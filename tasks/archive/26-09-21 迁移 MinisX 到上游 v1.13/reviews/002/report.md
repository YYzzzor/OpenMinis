# Independent review

Snapshot: a1f2d470b5856ea2dd02424bf7ad023d2a8bcda66b3aa710a1fc1239626c4651
Conclusion: nonblocking

## Coverage
Required Spec sections read: docs/specs/ios-device-data-capabilities.md section 1 文档定位与核查基线, section 2 Agent 接口与苹果系统 API 的关系, section 3 个人数据读写, section 6 授权、缺失能力与容易混淆的范围 (required excerpts given in request.md); docs/specs/ios-calendar-recurrence.md full file; docs/harness/record-formats.md full file; AGENTS.md full file; .agents/skills/review-task/SKILL.md full file. Task/evidence read: tasks/active/26-09-21 迁移 MinisX 到上游 v1.13/task.md, verification.md, reviews/001/resolution.md, calendar-results.json, calendar-26.4-r1.txt (head and tail), calendar-27.0-r1.txt, harness-validation.md, harness-cli-smoke.json, harness-tests-final.txt, built-app-identity.json, branding-validation.json, upstream-bug-comparison.md, validation-transcripts.md. Optional references: none read (ios-sandbox-ish-summary.md sections 1/2/4 and debug-server-api.md left unread; no build/mount/debug path was in scope). Code read: patches/base-to-worktree.patch (full, including CalendarOffload.m/.h, RemindersOffload.m, NativeOffloadUtils.m, SoulStore.swift, ContentView.swift, AIChatViewModel.swift, MinisApp.swift, project.pbxproj, Info.plist, Localizable.xcstrings, MinisUserAgent.swift, build_ffmpeg.sh/build_ish.sh/build_lame.sh/build_rclone_ios.sh), snapshot/src/ios/CalendarTests/CalendarRecurrenceTests.m, snapshot/src/ios/NativeOffloads/CalendarOffload.m, snapshot/src/ios/Views/Settings/SoulSettingsView.swift, snapshot/src/ios/Views/Providers/ProviderInstanceDetailView.swift, snapshot/src/ios/Views/Chat/ChatInputBar.swift, snapshot/src/ios/Agent/MessageList/CollectionViewMessageListV3.swift, snapshot/.gitignore, snapshot/.ignore, snapshot/deps/ios_build_target.sh.

## Limitations
This review is static: I ran no build, no test, no simulator, and no app; all runtime claims come from the provided logs/summaries and were not re-executed. The calendar suite is not green: per calendar-results.json / calendar-26.4-r1.txt / calendar-27.0-r1.txt both iOS 26.4 and 27.0 simulators report 34 tests with 30 passed and 4 failed (testWeeksOfYear, testPositiveWeeksOfYear, the weeks-of-year combination inside testSetPositionsAcrossSelectors, testNativeWeekNumberReference); verification.md records this as the accepted weeksOfTheYear limitation, and real-device behavior is unverified, so any regression masked by those failures is unexamined. The two prior bug fixes (thinking retry commit-boundary, Unicode math fallback) are explicitly not migrated; upstream-bug-comparison.md rests on isolated probe logs, and no full AIChatViewModel/UI reproduction, real provider chat, Stop, or retry path was run. Full xcresult, screenshots (launch/about/terminal), harness-source-manifest and the external migration-evidence directory are not in this snapshot, so built-app identity, branding, iSH execution and UI startup were verified only through the supplied JSON/summary files. tasks/archive and docs/tasks bodies were not read (excluded by the request/manifest policy), so archive byte-integrity is taken from harness-validation.md rather than re-derived. Optional reference docs/specs/ios-sandbox-ish-summary.md sections 1/2/4 and docs/specs/debug-server-api.md were not read. Real device, code signing, upgrade/real iCloud data migration, TestFlight and provider/network behavior were not verified. The R1 fix from review 001 was checked by reading code and the supplied test logs (testReminderEntrypointsRejectCalendarRecurrenceWithoutMutation, testReminderRecurrenceOptionLikeTextRemainsLiteral) but I did not execute those tests myself.

## R1
- severity: low
- requirement: task.md 目标与验收: 应用名称、主要界面…产品名称统一显示 MinisX; 默认 Minis 名称按现有规则显示 MinisX (requirements/tasks/active/26-09-21 迁移 MinisX 到上游 v1.13/task.md).
- location: snapshot/src/ios/Agent/Session/SoulStore.swift:928-934 (identitySection reads metadata.name and falls back to "Minis"), versus the new displayName mapping at SoulStore.swift:512-515 and the UI call sites that use it (e.g. Agent/MessageList/CollectionViewMessageListV3.swift:245, Views/ContentView.swift:1000, Views/Chat/AIChatView.swift:461).
- trigger: Any session with the default SOUL.md (name "Minis"): the first system-prompt sentence built by SystemPromptBuilder.identitySection() stays "You are Minis, …", while the chat title, typing indicator, input placeholder and Live Activity all render "MinisX"; the assistant therefore self-identifies as Minis in visible chat text that the app itself controls.
- evidence: SoulStore.swift:763 default content is `name: "Minis"`; SoulStore.swift:933 returns `n.isEmpty ? "Minis" : n`; AIChatViewModel.swift:1829 prepends SystemPromptBuilder.identitySection() to the system prompt; AIChatViewModel.swift:4811 confirms this path is always injected (not gated by memory toggle); the task's own branding-validation.json records `"soul_validation": "… and prompt identity unchanged; displayed legacy names mapped only in UI"`.
- verification: Static trace of identitySection() → AIChatViewModel system prompt plus the default SOUL.md content. Confirming the user-visible effect needs one default-config session on simulator/device (or dumping the assembled system prompt) to see whether the model calls itself Minis; not executed here.
- certainty: likely — the code behaviour is certain (default name reaches the prompt as "Minis"), but whether this counts as the 'display name' the acceptance item covers is a judgement, and branding-validation.json documents the untouched prompt identity as an intentional choice; main agent should confirm intent.

## R2
- severity: low
- requirement: task.md 目标与验收: 应用名称…产品名称统一显示 MinisX, and the migration must not leave self-contradictory product naming (this task's branding scope; the calendar Spec does not exempt outbound identifiers).
- location: snapshot/src/ios/Views/Providers/ProviderInstanceDetailView.swift:607 (new copy: "… Leave empty to use the MinisX default …") versus the actual default at snapshot/src/ios/Shared/MinisUserAgent.swift:26 (`let ua = "Minis/\(version) (iOS \(osVersion); \(model))"`) and the unchanged X-Title headers at Agent/Chat/AIChatViewModel+ProviderFactory.swift:163, Providers/LLMProviderFactory.swift:186, Providers/OpenRouter/OpenRouterModelsAPI.swift:21 and Providers/OpenRouter/OAuth/OpenRouterOAuthManager.swift:131 (all "Minis App").
- trigger: A user reads the User-Agent override hint in Settings and is told the default is MinisX, while requests actually send `Minis/1.13 …` as User-Agent and `Minis App` as X-Title (visible in OpenRouter/provider dashboards); the migration changed the describing string but not the described value.
- evidence: New catalog/Swift key "MinisX default" in ProviderInstanceDetailView.swift:607 and Localizable.xcstrings, against the untouched MinisUserAgent.default and the four untouched "Minis App" header literals listed above; none of MinisUserAgent.swift or the three header call sites appear in the changed-file list of branding-validation.json.
- verification: Source string comparison; confirming the outbound values would need a captured provider request (not run). Deliberately changing the User-Agent is a judgement call, but the app now contradicts its own settings copy.
- certainty: certain that the strings disagree (directly readable), medium on whether outbound identifiers are in the branding scope the user approved.

## R3
- severity: low
- requirement: task.md 目标与验收: 既有用户自定义助手名称和形象仍按用户配置显示; context 决定 '不修改磁盘身份、用户自定义名称或图标' (branding boundary recorded in reviews/001/resolution.md R2).
- location: snapshot/src/ios/Views/Settings/SoulSettingsView.swift:50-52 — `TextField("MinisX", text: Binding(get: { name == "Minis" ? "MinisX" : name }, set: { name = $0 }))`.
- trigger: With the default stored name "Minis", any edit+commit of the Settings → Soul 'Name' field invokes the setter with the displayed value, so SOUL.md's `name` is rewritten from "Minis" to "MinisX"; the mapping stops being display-only and mutates stored user content / the prompt identity (see R1).
- evidence: The get/set binding above writes the mapped value straight back to `name`; SoulStore.serialize (SoulStore.swift:615) emits `name: "\(file.metadata.name)"` verbatim into SOUL.md; the base v1.13 line was `TextField("Minis", text: $name)`, so the mapping is newly introduced by this migration.
- verification: Static reading of the binding and serializer. Confirming whether a no-change focus/commit also writes needs a simulator edit of SOUL.md and a file diff; not executed.
- certainty: likely for any actual edit (setter assigns unconditionally); unverified for commit-without-change, and the outcome may be judged as acceptable branding rather than a defect.
