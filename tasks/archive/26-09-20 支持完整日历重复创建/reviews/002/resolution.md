# 第二轮意见处置
原始报告结论 nonblocking，但每条 finding 缺 certainty，Harness 状态 incomplete；不能当作完成的独立审查。
首轮记录导出改变仓库状态，导致第二轮严格 export freshness 检查拒绝；核实仅新增 reviews/001 后逐字节保留第二轮原始材料，不改原状态和快照标识。

- R1 接受：验证报告当时暂存在当前任务工作目录，已补入项目 verification.md，列出环境、复现命令、矩阵、失败项及未验证范围。
- R2 接受：补充新接口文档的 Spec 索引入口；修正能力清单基线，明确原核查与本次源码变更的区别。仅文档导航/事实更新，无开发规则变更。
- R3 接受并缩小行为影响：不带 time-zone 的普通单次事件不主动写时区，沿用原 EventKit 默认行为；重复事件默认绑定设备当地时区。保留指定日历不存在时报错的必要修复，增加说明及默认时区回归测试。
- R4 接受：测试改为调用真正的 calendar_offload_handle 命令入口，补充非 create 使用重复参数、无效/缺失时区、起止时间错误、没有频率的星期参数等无写入验证。

仍未验收：年度周次在两个模拟器均不展开后续日期，直接 EventKit 对照和保存 20 分钟后的查询都复现；无真机。主 Agent 不降低该验收标准。

R3 的“原先为浮动时区”说法未获运行证据支持：iOS 26.4 实际默认返回 Asia/Shanghai。回归改用完全复制旧 EventKit 创建路径的原生对照，比较结果而不假定默认值为 nil。
