# Independent review

Snapshot: 0a5027baad38e6fa5058dcec85c9f92343402a1a38a7c3e71d2d8bfdb29be733
Conclusion: nonblocking

## Coverage
Required Spec sections read: requirements/docs/tasks/active/26-09-20 支持完整日历重复创建/task.md (full: 目标与验收, 上下文与决定, Spec 阅读清单, 实现计划, 进展与验证, 审查, 当前阻塞证据, 第二轮审查后的调整, 最终交接状态, 本轮授权后的复核, 005 后修复与当前状态); requirements/docs/tasks/active/26-09-20 支持完整日历重复创建/verification.md (full incl. 结论, 覆盖矩阵, 年度周次未通过项, 验证边界, 004 审查后时区解析修复验证, 005 审查后完整回归); requirements/docs/specs/ios-calendar-recurrence.md (full: 当前验收限制, 使用方式, Apple 公开接口覆盖表, 复杂示例, 回读结果, 官方依据); requirements/docs/specs/ios-device-data-capabilities.md §1 文档定位与核查基线 (incl. 与现有 Spec 的关系), §2 Agent 接口与苹果系统 API 的关系, §3 个人数据读写 (calendar row plus 文件夹挂载), and §6 周期性任务的三种不同含义; requirements/docs/specs/ios-sandbox-ish-summary.md §4 Native Offload System (4.1 Architecture, 4.2 Capabilities and Scope, 4.3 Shared Utilities). Optional/related evidence also read: patches/base-to-worktree.patch (full diff; staged.patch is empty and base-to-worktree equals unstaged.patch), snapshot/src/ios/NativeOffloads/CalendarOffload.h and CalendarOffload.m (create/recurrence/parse/handler paths, line 250-540, 815-960, 1445-1510), snapshot/src/ios/NativeOffloads/NativeOffloadUtils.m (noff_find_arg/noff_has_flag/noff_get_subcommand), snapshot/src/ios/CalendarTests/CalendarRecurrenceTests.m, snapshot/src/ios/Minis.xcodeproj/xcshareddata/xcschemes/MinisCalendarTests.xcscheme, project.pbxproj diff and target refs (E51000040, CA1E0007), AIChatViewModel.swift prompt diff, AGENTS.md index diff, snapshot/docs/tasks/.../reviews/005/report.md and 005/resolution.md. Excluded paths deps/ish and deps/proot were not opened.

## Limitations
No test was executed: the review used only read-only file tools and no Xcode, iOS simulator, or physical device is available here, so the 26 XCTest methods, the two-platform full regression, the xcodebuild reproduce commands, EventKit weeksOfTheYear BYWEEKNO expansion, time-zone/DST behavior, and the simulator calendar-permission grant flow are all unexecuted and independently unverified. All runtime claims (22/26 pass with the same 4 weeksOfTheYear failures and exit code 65 on iOS 26.4 and 27.0; direct-EventKit control) rest solely on verification.md logs and were not reproduced. Static review cannot prove runtime correctness: the argument scan, date/zone math, and EventKit save/read-back were traced by reading, not run. Required Spec dependencies were resolvable in this snapshot (task.md, verification.md, ios-calendar-recurrence.md, ios-device-data-capabilities.md, ios-sandbox-ish-summary.md all present and read); the historical review artifacts cited by task.md (reviews/001, 002, 004, 005) live under snapshot/docs/tasks/.../reviews and were only partly consulted (004/005), so prior wording for 001/002 was not checked. No real iPhone, iCloud/Exchange/CalDAV calendar service, or natural-language AI-to-shell session is in evidence. Excluded submodules deps/ish and deps/proot record Git state only and were not reviewed.

## R1
- severity: medium
- requirement: task.md 目标与验收 '[ ] 每个参数均经过实际原生命令创建、EventKit 保存和回读、展开日期验证' and ios-calendar-recurrence.md 当前验收限制 ('全部公开参数已接入，但"所有接口均可用"的验收尚未通过'; weeksOfTheYear only the first occurrence; 不得把规则保存成功或存在 recurrenceRules 当作重复生效).
- location: snapshot/src/ios/NativeOffloads/CalendarOffload.m parse_recurrence (weeks path ~line 380) and recurrence_warnings() (~line 486); snapshot/src/ios/CalendarTests/CalendarRecurrenceTests.m testWeeksOfYear, testPositiveWeeksOfYear, testSetPositionsAcrossSelectors, testNativeWeekNumberReference; verification.md 年度周次未通过项 and 005 审查后完整回归.
- trigger: apple-calendar create --title T --start 2027-01-13T09:00:00Z --end 2027-01-13T10:00:00Z --time-zone Etc/UTC --recurrence yearly --recurrence-weeks-of-year 2,-2 --recurrence-days-of-week WE --recurrence-count 4, then list over 2027-2029 (or the direct-EventKit control in testNativeWeekNumberReference).
- evidence: verification.md records that on iOS 26.4 and 27.0 the full suites are 22/26 pass with all 4 failures involving weeksOfTheYear (exit code 65), that a direct EKRecurrenceRule control reproduces it, and that the assertions are intentionally kept un-skipped; task.md keeps 状态：blocked and the acceptance checkbox open. The implementation accepts --recurrence-weeks-of-year and, when the read-back rule's weeksOfTheYear is non-empty, only attaches a warnings string via recurrence_warnings(); it neither makes the rule expand nor rejects the parameter. So the saved rule exists (is_recurring/recurrence_rules populated) while only the first occurrence is returned, which the Spec forbids treating as verified success.
- verification: Not independently reproduced (read-only review, no Xcode/simulator/device). Assessed from verification.md logs plus static reading of the weeks-path construction, the warning branch, and the retained failing test assertions.
- certainty: confirmed from repository evidence (documented, not independently reproduced)

## R2
- severity: low
- requirement: ios-calendar-recurrence.md '参数超范围、缺值、重复选项、拼错的 recurrence 选项及系统会忽略的频率/参数组合返回 invalid_args，不保存事件' together with the 004/005 R2 fix intent that only option-position tokens are treated as options so free-text values remain literal.
- location: snapshot/src/ios/NativeOffloads/CalendarOffload.m calendar_option_has_value() (~line 296), calendar_option_index() (~line 305), and the non-create recurrence guard in calendar_offload_handle (~line 1470); the omitted option is consumed at the call site noff_find_arg(argc, argv, "--parent-id") (CalendarOffload.m:1210).
- trigger: apple-calendar remind --title T --parent-id --help (or any --parent-id value beginning with --recurrence, e.g. remind --title T --parent-id --recurrence-foo).
- evidence: --parent-id is a real value-taking option (noff_find_arg at line 1210), but calendar_option_has_value()'s array is only @[--title, --start, --end, --calendar, --location, --notes, --alarm, --time-zone, --id, --occurrence-date, --span, --days, --limit, --due, --list, --priority], so the new position-aware scanner does not skip the token after --parent-id. Consequently calendar_option_index(..., "--help") matches the literal value and the handler prints help and returns success instead of the intended parent-id/normal path, and the non-create recurrence guard misreads a literal value beginning with --recurrence as a recurrence option. The new tests testTimeZoneOptionLikeValuesAreLiteral, testOtherOptionLikeTextValuesAreLiteral and testNonCreateRejectsRecurrenceOptions never exercise --parent-id, so this gap is untested.
- verification: Static tracing of calendar_option_has_value, calendar_option_index, calendar_offload_handle and the noff_find_arg("--parent-id") call site; not executed at runtime.
- certainty: confirmed by code reading
