# 26-09-15 统一 MinisX 界面名称
状态：done

## 恢复信息
分支：feat/minisx-branding；HEAD：141b9f2；保留已有模拟器构建适配与改名工作，未提交。
下一步：本次完成；未提交或推送。
待审批：无。按用户明确委托由主 Agent 判断，本次展示文案修改由主 Agent 校验，不发 Pi。

## 目标与验收
用户要求将界面中所有应用名称 Minis 改为 MinisX。
- [x] 首页、聊天、设置、文件、快捷指令、通知、权限说明和各语言文案改名。
- [x] 保留图标、Bundle ID、App Group、URL scheme、数据路径、真实项目/仓库地址。
- [x] 旧 SOUL 默认名称只映射显示，其他自定义名称与磁盘内容保持不变。
- [x] iOS27.0 模拟器构建成功，安装启动截图确认首页，核验产物中的权限文案。

## 实现与范围
本次为 iOS 展示层改名，不改 Android，不改变日历等 offload 行为。SOUL 默认文件/解析/同步种子仍保持原值，仅 displayName 为旧默认名称提供展示兼容。
真实协议名、源代码符号、URL、路径和用户内容里的 Minis 不属于应用品牌标签，保持原样。
不涉及 iSH/调试服务协议变更，无必读 Spec。

## 验证结果
- iOS27.0 Debug 本地签名构建 BUILD SUCCEEDED；日志 /tmp/MinisX-branding-releasecheck.log。
- 已安装并启动；截图 /tmp/MinisX-branding-ui.png 确认首页标题为 MinisX。用户已存在的会话标题及聊天内容保持原样，不作为应用标签替换。
- 7种编译后权限本地化文案均无旧品牌；catalog共1651条，键映射、语言覆盖和格式占位符检查通过。
- 旧SOUL默认名、自定义名、空名的实际Swift显示兼容检查通过；升级前后模拟器SOUL.md SHA256相同。
- FileProvider日志确认 display name updated to MinisX；NSFileProviderManager.h明确同标识add更新显示名。isHidden为iOS不可用属性，未使用；无需移除域或文件。
- 独立子代理局部静态核查发现Soul编辑页旧默认名会导致isDirty误判，已改成显示Binding映射，raw state与比较基线不变；复核通过。
- 补齐两处Agent给用户的闹钟首页名称指引；不改变协议和默认身份文件。
- git diff --check、资源JSON/plist/XML校验通过。未逐屏手动遍历全部语言或再次真机构建。

## 提交归档说明
相关源码已按用户要求提交为 6cc5ea9，未推送。文中“未提交”描述为当时执行阶段的历史状态。构建日志、截图、压缩快照及机器检查点保留本地，不随仓库提交；手写任务记录、验证结论和审查报告保留版本管理。
