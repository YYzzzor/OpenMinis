# 26-09-20 独立任务目录与按需归档
状态：done
更新：2026-09-20

## 恢复信息
分支：feat/minisx-branding；HEAD：0f1ba5f。
工作区：保留先前日历重复创建全部未提交改动；本次不改 App 源码。
下一步：本次目录迁移与默认读取隔离已验收，归档本任务。未提交、未推送。
待审批：无目录/规则设计待确认；用户已经明确要求根 tasks、完成任务归档且不被动读取。

## 目标与验收
- [x] docs/tasks 移为根目录 tasks，原已完成的 9 个任务归档，1 个 blocked 任务保留 active。
- [x] 同步规则、技能、文档链接、忽略规则，保留历史证据与检查点原字节。
- [x] 默认恢复/检查点/审查不会读取归档正文或在补丁中包含它；显式选择受控。
- [x] Harness 行为测试、迁移完整性及文档检查通过，独立审查问题已处理。

## 方案与决定
按状态决定归档，不根据年龄或提交推断完成。完成/取消后及时归档；恢复只发现 active 的标题与状态。旧任务树不进入泛代码快照和补丁，需求文件必须显式选择，防止迁移删除diff泄漏归档内容。归档读取需要明确历史用途，不能在 active 无匹配时自动遍历历史。

## Spec 阅读清单
必读：docs/harness/proposals/006-task-layout-and-archive.md 全文。
必读：docs/harness/record-formats.md 全文；docs/harness/operations.md 全文。
参考：docs/harness/design.md 文件与职责、恢复协议、审查协议。

## 实施与验证
原目录 10 个任务、119 个文件、5706579 字节；已迁移。102 个审查/证据/检查点文件原字节校验通过；普通任务文档仅修复维护中的链接和路径。旧历史提案与快照路径保留，用新提案说明替代关系。

## 当前验证
- Harness 全量 42 项测试运行成功，1 项既有非 UTF-8 文件名用例在 macOS 文件系统不支持时按原逻辑跳过，其余 41 项通过；没有失败。首次沙箱运行的同项权限失败保留在本机工作记录，升级到正常文件系统权限后确认平台跳过。
- 新增默认隔离、显式选择、迁移删除补丁、暂存 blob 漂移、归档冲突检测、旧 bundle 兼容测试。
- 根 .ignore 已实际验证：普通 rg --files tasks 不含 archive，显式指定归档文件可读，git check-ignore 确認任务正文仍可跟踪。
- 维护中文档链接检查无断链；102 个历史审查/证据/检查点文件迁移后逐字节一致。
- 独立子 Agent 已检查工具与当前规则一致性，无阻断问题；仍准备按 Harness 进行 Pi 固定快照审查。
- App 源码与本任务前版本一致，未重复 iOS 构建。

## 最终验收与归档
Pi 001：reviewed / nonblocking；主 Agent 已核实并处理 3 条建议，详见 reviews/001/resolution.md。只修正了 freshness 文档、补充 migration.json、兼容旧归档忽略规则；脚本与测试源文件哈希仍匹配已审快照。针对性验证确认新旧归档默认搜索均隐藏、Git 仍可跟踪，119 文件当前 SHA 匹配、102 历史证据原字节不变。

用户明确授权本次 21 文件 Harness 快照发送给 DeepSeek 后才执行 Pi；不含 App 或归档正文。初次自动审批拒绝记录是历史过程，新授权已解除本次外发限制。

本次任务状态 done，整体移入 tasks/archive/。原有 9 个 done 加本任务共 10 个归档，active 保留 1 个旧 blocked 任务。原始审查请求和 manifest 路径不因本次归档重写。
