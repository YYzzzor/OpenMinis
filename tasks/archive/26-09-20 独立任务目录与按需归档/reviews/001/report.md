# Independent review

Snapshot: 2908c1d5012c7bb5ae14bbef4a0e4692dc37849567b4c706aec8f6f5a4d59e4d
Conclusion: nonblocking

## Coverage
Required Spec/record sections read in full from the bundle: requirements/tasks/active/26-09-20 独立任务目录与按需归档/task.md (全文: 恢复信息, 目标与验收, 方案与决定, Spec 阅读清单, 实施与验证, 当前验证) and .../verification.md (全文: 自动测试, 迁移与入口, 证据范围); requirements/tasks/README.md (全文: 任务记录/生命周期与默认读取范围); requirements/docs/harness/proposals/006-task-layout-and-archive.md (全文: 修改与影响, 验证与撤销); requirements/docs/harness/record-formats.md (全文: 任务命名, 任务记录, 每轮审查, 共用规则提案, 生命周期与默认读取范围); requirements/docs/harness/operations.md (全文: 恢复和检查点, 准备审查, 按小节提供 Spec, 调用 Pi, 核验与导出, 验证, 任务发现与归档). Optional/reference material read: docs/harness/design.md (文件与职责, 恢复协议, 审查协议 including 输入绑定; plus 目标和边界/纠偏 sections in the same file), docs/harness/skills-guide.md (resume-task 与 review-task 段, 实际使用时怎么衔接), AGENTS.md (Development Harness bullets and Spec Index table), .agents/skills/{plan-task,resume-task,review-task}/SKILL.md, .ignore 与 .gitignore, and the reviewed code/artifacts themselves: scripts/harness/review.py (full), scripts/harness/task_state.py (full), scripts/harness/spec_context.py (full, unchanged), scripts/harness/test_review.py, test_task_state.py, test_spec_context.py (method inventory), patches/base-to-worktree.patch (= unstaged.patch; staged.patch empty), request.md and manifest.json. Sections/claims not read were excluded paths (listed in limitations).

## Limitations
No test or command was executed in this review; the claims 'Ran 42 tests OK (skipped=1)', '普通 rg --files tasks 不含 archive', 'git check-ignore 确认', and '维护中文档链接检查无断链' come from verification.md/task.md only and were not reproduced (the snapshot does contain exactly 42 test methods, consistent with the reported count). The reviewed policy excludes the entire tasks/ and docs/tasks/ trees from snapshot files, status and patches, so I could not verify: whether docs/tasks/ is really gone, the 9-archived/1-blocked split of 10 tasks, 119 files / 5706579 bytes moved, the '102 review/evidence/checkpoint files byte-identical' claim, the old->new rollback mapping promised by proposal 006, the existence of tasks/active/26-09-11 实现 OpenMinis 开发 Harness/task.md referenced by docs/harness/design.md:33 and operations.md:10-11, or the .ignore/rg behaviour itself. Excluded paths were not read and remain unresolved dependencies referenced by the requirements I did read: docs/harness/validation.md (referenced by operations.md 验证 and design.md 工具适配), docs/harness/proposals/001-005, docs/harness/evidence (evidence/tests-full.log), docs/architecture, docs/specs (including ios-device-data-capabilities.md and the untracked ios-calendar-recurrence.md named by a Spec-Index row added in this patch), src/**, .github, notes and the excluded scripts. The Pi call itself (model behaviour, timeout/incomplete handling, pi --version) was not exercised and cannot be validated by static reading; `git diff --check` was not run. The bundle also mixes work belonging to the separate in-flight calendar task (AGENTS.md Spec-Index row; docs/specs excluded), which the task record declares as retained prior work but which the current task's requirements do not cover, so attribution of that hunk is out of scope. Reviewing this bundle is not proof of runtime correctness.

## R1
- severity: low
- requirement: requirements/docs/harness/operations.md 核验与导出: '导出本身会改变仓库状态，因此之后的严格 freshness 检查会报告变化。这是保守检测：主 Agent 需确认仅新增了报告，不能据此重写原审查标识。' (combined with the export example '--output <任务目录/reviews/001>' and record-formats 每轮审查 'reviews/001-request.md ...'), together with proposal 006 修改与影响 'review 默认把任务树排除在泛快照/补丁外'.
- location: scripts/harness/review.py:118 (files filter), 132-136 (status pathspec scoping), 268 (verify passes the stored policy); docs/harness/operations.md:70-76; snapshot/.gitignore:151.
- trigger: Prepare and run a round for a task record under tasks/ (e.g. tasks/active/X/task.md), export it to the documented location inside the task directory (tasks/<task>/reviews/001), then run `review.py check <bundle> --repo <repo>`.
- evidence: With task_record_policy='selected-only', state() drops every path under TASK_ROOTS from `files` unless it is one of the requirements, and its status call is limited to `-- . :(literal,exclude)tasks :(literal,exclude)docs/tasks` plus only the explicitly selected record paths; the exported request.md / manifest.json / report.txt / report.json / report.md / status.json created under tasks/<task>/reviews/001 therefore never enter the recomputed state, so verify() returns success instead of the 'stale' error the operations guide tells the main agent to expect. The documented conservative signal for 'the bundle's inputs changed because export happened' is silently gone for the documented output location.
- verification: Add a regression test: prepare a task record under tasks/active/, run() with the fake pi, export() into tasks/<task>/reviews/001, then assert review.verify(bundle, repo) raises 'stale' (it currently does not). If the new behaviour is intended, update the operations.md paragraph so the main agent does not rely on a freshness failure it will never see, and state explicitly that task-tree mutations are outside the freshness binding.
- certainty: high — traced end-to-end through state()'s files/status filtering and verify(); the only open question is whether the paragraph was meant to be revised.

## R2
- severity: medium
- requirement: proposal 006 验证与撤销: '回退可依据任务内迁移映射移动原位置并还原入口改动，不能覆盖期间新增工作' and '检查迁移数量、完整性、维护中文档链接、默认读取与显式选择行为 …'; task.md 目标与验收 item 1 ('原已完成的 9 个任务归档，1 个 blocked 任务保留 active') and 实施与验证 ('原目录 10 个任务、119 个文件、5706579 字节；已迁移。102 个审查/证据/检查点文件原字节校验通过').
- location: requirements/tasks/active/26-09-20 独立任务目录与按需归档/task.md (## 实施与验证, ## 当前验证), .../verification.md (## 迁移与入口), request.md requirement list, scripts/harness/review.py:58-62/118/134/198.
- trigger: Trying to confirm migration completeness or to execute the documented rollback using only the reviewed material, or asking an independent reviewer to corroborate the 9/1 split, the 119-file/5706579-byte/102-file claims, or the link validity claim.
- evidence: Neither of the two selected task records contains an old->new path mapping or a per-file inventory; they contain only aggregate counts (10 tasks, 119 files, 5706579 bytes, 102 files, 42 tests) and pointers to evidence/tests-full.log, which was not selected. The bundle deliberately excludes both task trees from files/status/patches (manifest.state.status lists only the two selected 26-09-20 records as untracked), and request.md's requirement list contains only the six files it names, so no reviewed artifact either supports or contradicts the migration claims, and the rollback mapping the proposal relies on is absent from everything a reviewer or a later main agent can read by default.
- verification: Generate a machine-checkable artifact and select it explicitly (e.g. `git diff --name-status -M <base> -- docs/tasks tasks` plus per-file SHA-256, stored in the task directory and passed with --spec), or write the mapping into task.md's 实施与验证 section before the task is archived, then re-verify the counts against it.
- certainty: high that the two selected records contain no mapping or inventory; medium overall, because an unselected sibling attachment could hold one and the selected-only policy deliberately does not load it.

## R3
- severity: low
- requirement: proposal 006 修改与影响 '.ignore 默认隔离归档搜索，Git 仍跟踪归档'; AGENTS.md:124 'Do not recursively search, read, follow links into, or attach `tasks/archive/` as ambient context'; operations.md:89 '根 .ignore 也让普通 rg 搜索和文件发现默认略过 archive'; record-formats.md 生命周期与默认读取范围 'archive 不参与被动搜索、正文加载、附件递归或背景收集'.
- location: snapshot/.ignore:2 (only rule `/tasks/archive/`) versus scripts/harness/task_state.py:30 ARCHIVE_ROOTS = ('tasks/archive','docs/tasks/archive') and scripts/harness/review.py:58 TASK_ROOTS = ('tasks','docs/tasks'); snapshot/.gitignore:145-151 (the docs/tasks patterns were removed).
- trigger: A legacy or rolled-back repository state in which docs/tasks/archive/ exists (the rollback described in 006 验证与撤销 restores the old locations): ordinary discovery such as `rg --files`, and the agent's find/ls tools, then list archived task bodies again, while task_state and review still classify those paths as archive and require --include-archive.
- evidence: The ignore rule covers only the new archive root, whereas both scripts and the specs treat 'docs/tasks' / 'docs/tasks/archive' as a supported legacy task/archive root; the compatibility support was kept in code and in the operations/record text but not mirrored into the ignore rule, so the 'archive is never discovered passively' guarantee holds only for the post-migration path and silently weakens after a rollback.
- verification: In a scratch repository create docs/tasks/archive/x/task.md and run `rg --files` (and a find-based listing) with the current .ignore; expect it to be hidden. Either add `/docs/tasks/archive/` (and, if desired, `/docs/tasks/`) to .ignore, or record in the proposal that legacy docs/tasks trees are intentionally not covered by discovery isolation.
- certainty: medium — the gap between the single ignore rule and the two roots defined in code is certain; whether a legacy docs/tasks tree may legitimately reappear (per the rollback plan) is a policy decision for the main agent.
